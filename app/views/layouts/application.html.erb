<!DOCTYPE html>
<html>
  <head>
    <title>CheckinManager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="container">
    <%= render 'shared/navbar' if hotel_signed_in? %>
    <%= render 'shared/flash' %>
    <div class="content-container container">
      <%= yield %>
    </div>
  </div>
  <%= render 'shared/modal' %>
  </body>
</html>
